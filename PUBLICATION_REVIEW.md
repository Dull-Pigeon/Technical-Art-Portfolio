# Repository publication check

Deployment-only check, 2026-09-17. Existing portfolio content was not reviewed or rewritten.

Included: static templates, JSON, CSS, lightweight player, four optimized WebP stills, downloadable Breakdown, GitHub Pages workflow and maintenance instructions. Generated HTML is required preview output; no caches or QA artifacts are included.

Excluded: all MP4/video binaries and encoded video, original recording, Unity project, Asset Store weapon sources, other third-party source assets, credentials, local environment values and temporary Work files. The reviewed snapshot is uploaded without earlier local Git history. The remote's initial commit contains only its generated README.

Checks: common credential patterns and local absolute paths in tracked text and DOCX XML; forbidden file types; file inventory and sizes; renderer syntax; internal routes at both domain root and GitHub Pages base path. Previous image/document metadata inspection remains applicable. This heuristic scan cannot guarantee detection of every secret format; review future additions.
